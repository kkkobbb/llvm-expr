# サンプル 宣言関係

# int型 初期値3の変数
decl gl_var -> int;
gl_var <- 3;

# int型 変数宣言
decl ex_var -> int;

# 戻り値int 引数int2つの関数宣言
decl func -> int(int, int);
func <- (a, b) # 上記関数の定義
{
	return 3;
}


# 戻り値、引数なし関数
decl fff -> void();
fff <- () 1 + 2;


decl func <- int(int, int, int);
f <- (a, b, c)
	if a then
		b;
	else
		c;
	end;

